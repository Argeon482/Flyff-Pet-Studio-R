import React from 'react';

// This component is obsolete after the profile system was replaced with a
// code-based save/load system. It can be safely deleted from the project.
const ProfileModal: React.FC = () => {
  return null;
};

export default ProfileModal;